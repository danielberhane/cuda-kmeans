# Build artifacts
build/
cuda_kmeans
*.o
gmon.out

# Output files
*.cluster_centres
*.membership

# Editor files
*~
\#*\#
*.swp
*.swo

# Benchmark binaries
cuda_kmeans_bench
seq_kmeans
seq_kmeans_omp
gen_points

# Benchmark outputs and generated datasets.
# gen_points is deterministic given (seed, N, D), so the scaling datasets are
# reproducible from gen_points and are not committed -- at N=5M they are
# ~500 MB each.
bench/results/
bench/data/
*.seq_centres
